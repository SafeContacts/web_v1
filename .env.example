# Required for deployment
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/safecontacts
JWT_SECRET=generate_a_strong_random_secret_at_least_32_chars

# Optional: network search depth (default 2 = 1st + 2nd level)
# NETWORK_SEARCH_DEPTH=2

# Optional: WhatsApp Business API (set to true to enable in UI)
# NEXT_PUBLIC_WABA_ENABLED=false

# Optional: encryption for sensitive data (use 32-char key in production)
# ENCRYPTION_KEY=your_32_character_encryption_key!!
